{% extends 'base.html.twig' %} {% block title %}Blog - {{ article.title }} {% endblock %} {% block body %}
<div class="p-5 bg-light">
    <div class="container">
        <h1 class="display-3">{{ article.title }}</h1>
        <p class="lead">{{ article.author }}</p>
        <p class="lead">{{article.createdAt | date('d-m-Y')}}</p>

        <a href=" {{ path('app_blog') }} " class="btn btn-primary">Retour aux articles</a
    >
    <!-- Pr trouver le nom de notre page, il suffit d'aller sur le Controller correspondant et ds la ligne de la route et regarder à l'endroit : name="nomRoute" -->
  </div>
</div>

{% endblock %}