{% extends 'base.html.twig' %}

{% block title %}New Food{% endblock %}

{% block body %}
    <h1>Create new Food</h1>

    <div class="container">
        {{ include('food/_form.html.twig') }}

        <a href="{{ path('app_food_index') }}" class="btn btn-success">Voir toutes les recettes</a>
    </div>

{% endblock %}
